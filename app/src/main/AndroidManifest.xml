<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.obama.venus">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />


    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main_menu"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".food_list"
            android:label="@string/title_activity_food_list"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="portrait"/>
        <activity android:name=".cinema"
            android:screenOrientation="portrait"/>
        <activity android:name=".transfer"
            android:screenOrientation="portrait"/>
        <activity android:name=".information"
            android:screenOrientation="portrait"/>
        <activity android:name=".privacy"
            android:screenOrientation="portrait"/>
        <activity android:name=".vcoin"
            android:screenOrientation="portrait"/>
        <activity android:name=".edit"
            android:screenOrientation="portrait"/>
        <activity android:name=".cupon"
            android:screenOrientation="portrait"/>
        <activity android:name=".store"
            android:screenOrientation="portrait"/>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".maps"
            android:screenOrientation="portrait"
            android:label="@string/title_activity_maps" />
        <activity android:name=".car"
            android:screenOrientation="portrait"/>
        <activity android:name=".qrcode"
            android:screenOrientation="portrait"/>
        <activity android:name=".gift"
            android:screenOrientation="portrait"/>
        <activity android:name=".gift_item"
            android:screenOrientation="portrait"/>
        <activity android:name=".gift_box"
            android:screenOrientation="portrait"/>
        <activity android:name=".record"
            android:screenOrientation="portrait"/>
        <activity android:name=".shop_record"
            android:screenOrientation="portrait"/>
        <activity android:name=".gift_record"
            android:screenOrientation="portrait"/>

        <service
            android:name=".MyService"
            android:enabled="true" />

        <activity android:name=".gift_box_used"
            android:screenOrientation="portrait"></activity>
    </application>

</manifest>